#!/bin/sh
set -eu

runtime_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec "${KUBECODE_NODE_PATH:?KUBECODE_NODE_PATH is required}" \
  "$runtime_root/shared/adapter-runtime/claude-agent-acp.mjs" \
  "$@"
